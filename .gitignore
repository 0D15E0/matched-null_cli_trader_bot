.env
build/
/data/
/state/
__pycache__/
*.pyc
/logs/
/logs_ensemble/
/state_ensemble/
/archive/
/state_live/
/logs_live/
experiments/**/.wf_cache*.json

# Pi runtime dirs: the systemd units write here, so an accidental
# `git add -A` on the host would otherwise commit live logs and state.
logs_pi/
state_pi/
state_paper/
# local tooling / OS cruft
.venv/
.DS_Store
.claude/
# Research trail and roadmap: kept locally, not published. These carry the
# real account's size and P&L, and a working-notes level of polish that the
# published docs deliberately do not have.
PROFITABILITY_PLAN.md
VOLATILITY_TRADING_PLAN.md
TODO.md
cli_trader-old-history.bundle