# Local runtime configuration template.
# Copy this file to deploy/pi/bot.env on the target host; do not commit the copy.
# Start in paper mode and replace values only after the development checks pass.
MODE=paper
STATE_DIR=state_paper
EQUITY=1000
QUOTE_CAP=1000
VOL_TARGET=0.20
VOL_WINDOW=30
POLL_INTERVAL=120
STRATEGY=ensemble_vote
PERIOD=14400
EXTRA_FLAGS=
ADOPT=